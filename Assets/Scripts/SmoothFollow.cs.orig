using UnityEngine;
using System.Collections;

public class SmoothFollow : MonoBehaviour {
	public Transform target;
	public Vector3 offset;
	void Update () {
		transform.position = new Vector3((int)(target.position.x - 160),((int)target.position.y - 120),transform.position.z);
	}
}
